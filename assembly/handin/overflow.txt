pushl %ebp				# push returnpointer to the stack. (This is a function)
movl %esp, %ebp
subl $4, %esp			# I need 1 local variables

movl 8(%ebp),%eax		# Get the first parameter
movl %ebx, -8(%ebp)		# Must save this




movl -8(%ebp), %ebx			# Restore this
movl $0, %eax				# Result

leave
ret
